name: ci pipeline

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:

  build:
    runs-on: ubuntu-latest
    permissions:
      contents: read

    steps:
      - uses: actions/checkout@v4
      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'

      - name: Set up gradle
        uses: gradle/actions/setup-gradle@417ae3ccd767c252f5661f1ace9f835f9654f2b5 # v3.1.0

      - name: give permission to gradle
        run: chmod +x gradlew

      - name: build with gradle wrapper
        run: ./gradlew clean build

      - name: Install Google Cloud SDK
        run: |
            curl -O https://dl.google.com/dl/cloudsdk/channels/rapid/downloads/google-cloud-sdk-375.0.0-linux-x86_64.tar.gz
            tar zxvf google-cloud-sdk-375.0.0-linux-x86_64.tar.gz google-cloud-sdk
            ./google-cloud-sdk/install.sh

      - name: Authenticate with Google Cloud
        run: |
              echo ${{ secrets.GCP_SA_KEY }} | base64 --decode > /tmp/gcp-key.json
              ./google-cloud-sdk/bin/gcloud auth activate-service-account --key-file=/tmp/gcp-key.json
              ./google-cloud-sdk/bin/gcloud auth configure-docker

      - name: Build Docker image with Jib
        run: ./gradlew jib --image=gcr.io/${{ secrets.GCP_PROJECT_ID }}/cicd:latest

